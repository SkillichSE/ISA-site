diff --git a/admin.html b/admin.html
index bc41fb8..4b34488 100644
--- a/admin.html
+++ b/admin.html
@@ -444,6 +444,7 @@ function pageLaunches() {
   const statusMap = { open:'Open', upcoming:'Upcoming', closed:'Closed', full:'Full' };
   const items = data.launches.length ? data.launches.map(l=>{
     const pct = l.slots_total > 0 ? Math.round(l.slots_taken/l.slots_total*100) : 0;
+    const dateLabel = formatLaunchListDate(l.date, l.window_end);
     return `<div class="item-card">
       <div class="item-thumb-icon">🛰️</div>
       <div class="item-body">
@@ -452,7 +453,7 @@ function pageLaunches() {
           <span class="badge badge-${l.status}">${statusMap[l.status]||l.status}</span>
           ${l.published ? '<span class="badge badge-active">Published</span>' : '<span class="badge badge-inactive">Draft</span>'}
         </div>
-        <div class="item-meta">${l.orbit} · ${l.date || '—'}${l.mission_id ? ' · Mission: ' + l.mission_id : ''}</div>
+        <div class="item-meta">${l.orbit} · ${dateLabel}${l.mission_id ? ' · Mission: ' + l.mission_id : ''}</div>
         <div class="item-desc">${esc(mdToPlain(l.description))}</div>
         <div class="slots-bar"><div class="slots-fill" style="width:${pct}%"></div></div>
       </div>
@@ -570,7 +571,7 @@ function ta(id, lbl, val='') {
     </div>
     <textarea id="f-${id}">${esc(String(val))}</textarea>
     <div class="md-preview" id="mdprev-${id}"></div>
-    <div class="form-hint">Поддерживается Markdown — **жирный**, _курсив_, # заголовки, - списки, [текст](ссылка), &gt; цитата, \`код\`.</div>
+    <div class="form-hint">Markdown supported — **bold**, _italic_, # headings, - lists, [text](link), &gt; quote, \`code\`.</div>
   </div>`;
 }
 
@@ -598,7 +599,7 @@ function mdWrap(id, before, after) {
   const t = document.getElementById('f-'+id);
   if (!t) return;
   const start = t.selectionStart, end = t.selectionEnd;
-  const selected = t.value.slice(start, end) || 'текст';
+  const selected = t.value.slice(start, end) || 'text';
   t.value = t.value.slice(0, start) + before + selected + after + t.value.slice(end);
   t.focus();
   t.selectionStart = start + before.length;
@@ -622,8 +623,8 @@ function mdLink(id) {
   const t = document.getElementById('f-'+id);
   if (!t) return;
   const start = t.selectionStart, end = t.selectionEnd;
-  const selected = t.value.slice(start, end) || 'текст ссылки';
-  const url = prompt('Адрес ссылки (URL):', 'https://');
+  const selected = t.value.slice(start, end) || 'link text';
+  const url = prompt('Link URL:', 'https://');
   if (url === null) return;
   const md = `[${selected}](${url})`;
   t.value = t.value.slice(0, start) + md + t.value.slice(end);
@@ -636,6 +637,34 @@ function esc(s) { return s.replace(/&/g,'&amp;').replace(/"/g,'&quot;').replace(
 function g(id) { return (document.getElementById('f-'+id)||{}).value?.trim()||''; }
 function arr(id) { return g(id).split(',').map(s=>s.trim()).filter(Boolean); }
 
+// datetime-local inputs work in the browser's local wall-clock time and have
+// no timezone info of their own, while launches are stored in Supabase as
+// UTC timestamptz. These convert between the two so the admin always enters
+// times in their own local clock and visitors see it converted to theirs.
+function toLocalDatetimeInput(iso) {
+  if (!iso) return '';
+  const d = new Date(iso);
+  if (isNaN(d.getTime())) return '';
+  const pad = n => String(n).padStart(2, '0');
+  return `${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}T${pad(d.getHours())}:${pad(d.getMinutes())}`;
+}
+function fromLocalDatetimeInput(value) {
+  if (!value) return null;
+  const d = new Date(value);
+  return isNaN(d.getTime()) ? null : d.toISOString();
+}
+function formatLaunchListDate(dateIso, windowEndIso) {
+  if (!dateIso) return '—';
+  const start = new Date(dateIso);
+  if (isNaN(start.getTime())) return '—';
+  const startStr = start.toLocaleString(undefined, { year:'numeric', month:'short', day:'numeric', hour:'2-digit', minute:'2-digit' });
+  if (!windowEndIso) return startStr;
+  const end = new Date(windowEndIso);
+  if (isNaN(end.getTime())) return startStr;
+  const endStr = end.toLocaleTimeString(undefined, { hour:'2-digit', minute:'2-digit' });
+  return `${startStr} – ${endStr}`;
+}
+
 function buildForm(type, item) {
   const v = (k,d='') => item ? (item[k]??d) : d;
   if (type==='missions') return `
@@ -655,7 +684,8 @@ function buildForm(type, item) {
     <div class="form-row-2">${field('id','ID / Slug',v('id'),'e.g. launch-003')}${sel('published','Published',v('published')?'true':'false',['true','false'])}</div>
     ${field('name','Mission Name',v('name'))}
     <div class="form-row-2">${field('mission_id','Mission ID',v('mission_id'),'Optional - which mission this belongs to')}${sel('status','Status',v('status','open'),['open','upcoming','closed','full'])}</div>
-    <div class="form-row-2">${sel('orbit','Orbit',v('orbit','LEO'),['LEO','MEO','GEO','SSO','HEO'])}${field('date','Launch Date',v('date'),'YYYY-MM-DD')}</div>
+    <div class="form-row-2">${sel('orbit','Orbit',v('orbit','LEO'),['LEO','MEO','GEO','SSO','HEO'])}${field('date','Launch Window Start',toLocalDatetimeInput(v('date')),'Your local time','datetime-local')}</div>
+    ${field('window_end','Launch Window End',toLocalDatetimeInput(v('window_end')),'Optional — leave empty for a single point-in-time launch','datetime-local')}
     ${ta('description','Description',v('description'))}
     <div class="form-row-2">${field('slots_total','Total Slots',v('slots_total',4),'','number')}${field('slots_taken','Slots Taken',v('slots_taken',0),'','number')}</div>`;
   if (type==='news') return `
@@ -670,7 +700,7 @@ function buildForm(type, item) {
 function getForm(type) {
   if (type==='missions') return { id:g('id'),name:g('name'),division:g('division'),status:g('status'),description:g('description'),tags:arr('tags'),image:g('image') };
   if (type==='careers') return { id:g('id'),title:g('title'),program:g('program'),type:g('type'),description:g('description'),skills:arr('skills') };
-  if (type==='launches') return { id:g('id'),name:g('name'),mission_id:g('mission_id'),orbit:g('orbit'),status:g('status'),date:g('date'),description:g('description'),slots_total:parseInt(g('slots_total'))||0,slots_taken:parseInt(g('slots_taken'))||0,published:g('published')==='true' };
+  if (type==='launches') return { id:g('id'),name:g('name'),mission_id:g('mission_id'),orbit:g('orbit'),status:g('status'),date:fromLocalDatetimeInput(g('date')),window_end:fromLocalDatetimeInput(g('window_end')),description:g('description'),slots_total:parseInt(g('slots_total'))||0,slots_taken:parseInt(g('slots_taken'))||0,published:g('published')==='true' };
   if (type==='news') return { id:g('id'),tag:g('tag'),title:g('title'),date:g('date'),body:g('body'),image:g('image'),link:g('link') };
 }
 
diff --git a/launches.css b/launches.css
index 780ba65..cf1631b 100644
--- a/launches.css
+++ b/launches.css
@@ -86,6 +86,12 @@
   letter-spacing: -0.2px;
   line-height: 1.3;
 }
+.launch-time {
+  font-size: 0.78rem;
+  font-weight: 600;
+  color: var(--muted);
+  letter-spacing: 0.2px;
+}
 .launch-desc {
   font-size: 0.82rem;
   color: var(--muted);
diff --git a/launches.html b/launches.html
index 2af3d88..01dfbc2 100644
--- a/launches.html
+++ b/launches.html
@@ -61,6 +61,21 @@
       return map[s] || 'launch-status-scheduled';
     }
 
+    function timeRangeStr(dateIso, windowEndIso) {
+      if (!dateIso) return '';
+      const start = new Date(dateIso);
+      if (isNaN(start.getTime())) return '';
+      const startTime = start.toLocaleTimeString(undefined, { hour: '2-digit', minute: '2-digit' });
+      const tz = Intl.DateTimeFormat(undefined, { timeZoneName: 'short' }).formatToParts(start)
+        .find(p => p.type === 'timeZoneName')?.value || '';
+      const end = windowEndIso ? new Date(windowEndIso) : null;
+      if (end && !isNaN(end.getTime())) {
+        const endTime = end.toLocaleTimeString(undefined, { hour: '2-digit', minute: '2-digit' });
+        return `${startTime} – ${endTime}${tz ? ' ' + tz : ''}`;
+      }
+      return `${startTime}${tz ? ' ' + tz : ''}`;
+    }
+
     let _launches = [];
 
     fetch(`${SB_URL}/rest/v1/launches?order=launch_date`, {
@@ -80,7 +95,7 @@
           const day     = date ? date.getDate() : '—';
           const month   = date ? date.toLocaleDateString('en-US', { month: 'short' }).toUpperCase() : '—';
           const year    = date ? date.getFullYear() : '';
-          const dateStr = date ? date.toLocaleDateString('en-US', { year:'numeric', month:'long', day:'numeric' }) : 'TBD';
+          const timeStr = timeRangeStr(l.date, l.window_end);
 
           return `
           <div class="launch-card reveal-target" data-launch="${i}">
@@ -97,6 +112,7 @@
                 ${l.mission_id ? `<span class="launch-mission-badge">${escHtml(String(l.mission_id))}</span>` : ''}
               </div>
               <h3 class="launch-title">${escHtml(l.name || 'Unnamed Launch')}</h3>
+              ${timeStr ? `<p class="launch-time">${escHtml(timeStr)}</p>` : ''}
               ${l.description ? `<p class="launch-desc">${escHtml(mdToPlain(l.description))}</p>` : ''}
               <button class="launch-read" data-launch="${i}">Details →</button>
             </div>
@@ -117,12 +133,13 @@
     function openModal(l) {
       const date    = l.date ? new Date(l.date) : null;
       const dateStr = date ? date.toLocaleDateString('en-US', { year:'numeric', month:'long', day:'numeric' }) : 'TBD';
+      const timeStr = timeRangeStr(l.date, l.window_end);
 
       document.getElementById('launch-modal-meta').innerHTML =
         `<span class="launch-status ${statusClass(l.status)}">${escHtml(l.status || 'scheduled')}</span>
          ${l.mission_id ? `<span class="launch-mission-badge" style="margin-left:8px">${escHtml(String(l.mission_id))}</span>` : ''}`;
       document.getElementById('launch-modal-date-big').textContent = date ? date.getDate() : '—';
-      document.getElementById('launch-modal-date-sub').textContent = dateStr;
+      document.getElementById('launch-modal-date-sub').textContent = timeStr ? `${dateStr} · ${timeStr}` : dateStr;
       document.getElementById('launch-modal-title').textContent = l.name || 'Unnamed Launch';
       document.getElementById('launch-modal-body').innerHTML = mdToHtml(l.description);
 
diff --git a/nav.html b/nav.html
index c84dced..e24ef69 100644
--- a/nav.html
+++ b/nav.html
@@ -8,7 +8,21 @@
       <a href="/careers.html">Careers</a>
       <a href="/astronauts/" class="nav-launch">Astronauts</a>
       <a href="/launchshare/" class="nav-launch">Launchshare</a>
+      <div class="nav-launches" id="nav-launches">
+        <button class="nav-launches-toggle" id="nav-launches-toggle" type="button" aria-haspopup="true" aria-expanded="false">
+          <span class="nav-launches-dot"></span>
+          <span>Upcoming Launches</span>
+          <svg class="nav-launches-caret" width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"></polyline></svg>
+        </button>
+        <div class="nav-launches-panel" id="nav-launches-panel">
+          <div class="nav-launches-list" id="nav-launches-list">
+            <div class="nav-launches-empty">Loading…</div>
+          </div>
+          <a href="/launches.html" class="nav-launches-all">All Upcoming Launches</a>
+        </div>
+      </div>
     </div>
+
     <button class="burger" id="burger" aria-label="Open menu">
       <span></span><span></span><span></span>
     </button>
diff --git a/shared.css b/shared.css
index 2d68621..e037aee 100644
--- a/shared.css
+++ b/shared.css
@@ -109,6 +109,128 @@ body {
 }
 .nav-links a:hover { color: var(--text); background: rgba(255,255,255,0.06); }
 .nav-links a.active { color: var(--text); }
+
+/* --- NAV: UPCOMING LAUNCHES DROPDOWN --- */
+.nav-launches { position: relative; }
+.nav-launches-toggle {
+  display: flex;
+  align-items: center;
+  gap: 8px;
+  font-family: var(--font);
+  color: var(--muted);
+  background: none;
+  border: none;
+  font-size: 0.88rem;
+  font-weight: 500;
+  padding: 7px 14px;
+  border-radius: 8px;
+  cursor: pointer;
+  letter-spacing: 0.2px;
+  transition: color 0.2s, background 0.2s;
+}
+.nav-launches-toggle:hover { color: var(--text); background: rgba(255,255,255,0.06); }
+.nav-launches.open .nav-launches-toggle { color: var(--text); background: rgba(255,255,255,0.06); }
+.nav-launches-dot {
+  width: 6px; height: 6px;
+  border-radius: 50%;
+  background: #22c55e;
+  box-shadow: 0 0 0 3px rgba(34,197,94,0.18);
+  flex-shrink: 0;
+}
+.nav-launches-caret { transition: transform 0.2s; flex-shrink: 0; }
+.nav-launches.open .nav-launches-caret { transform: rotate(180deg); }
+
+.nav-launches-panel {
+  position: absolute;
+  top: calc(100% + 10px);
+  right: 0;
+  width: 340px;
+  background: rgba(15,15,15,0.98);
+  backdrop-filter: blur(16px);
+  -webkit-backdrop-filter: blur(16px);
+  border: 1px solid var(--line2);
+  border-radius: 16px;
+  padding: 8px;
+  opacity: 0;
+  transform: translateY(-8px);
+  pointer-events: none;
+  transition: opacity 0.18s, transform 0.18s;
+  box-shadow: 0 20px 50px rgba(0,0,0,0.5);
+  z-index: 1001;
+}
+.nav-launches.open .nav-launches-panel {
+  opacity: 1;
+  transform: translateY(0);
+  pointer-events: all;
+}
+.nav-launches-list { display: flex; flex-direction: column; }
+.nav-launch-item {
+  display: flex;
+  align-items: center;
+  gap: 12px;
+  padding: 10px 8px;
+  border-radius: 10px;
+  text-decoration: none;
+  color: var(--text);
+  transition: background 0.15s;
+}
+.nav-launch-item:hover { background: rgba(255,255,255,0.05); }
+.nav-launch-item + .nav-launch-item { border-top: 1px solid var(--line); margin-top: 2px; padding-top: 12px; }
+.nav-launch-glyph {
+  width: 44px; height: 44px;
+  flex-shrink: 0;
+  border-radius: 10px;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  font-size: 1.2rem;
+  background: linear-gradient(135deg, rgba(59,130,246,0.22), rgba(226,107,245,0.16));
+  border: 1px solid var(--line2);
+}
+.nav-launch-info { display: flex; flex-direction: column; gap: 2px; min-width: 0; flex: 1; }
+.nav-launch-name {
+  font-size: 0.85rem;
+  font-weight: 700;
+  letter-spacing: -0.1px;
+  overflow: hidden;
+  text-overflow: ellipsis;
+  white-space: nowrap;
+}
+.nav-launch-date { font-size: 0.75rem; color: var(--muted); }
+.nav-launch-tz { font-size: 0.7rem; color: var(--muted2); }
+.nav-launch-status {
+  flex-shrink: 0;
+  font-size: 0.62rem;
+  font-weight: 700;
+  letter-spacing: 0.6px;
+  padding: 3px 8px;
+  border-radius: 20px;
+  text-transform: uppercase;
+}
+.nav-launch-status-scheduled  { background: rgba(34,197,94,0.12);  color: #22c55e; }
+.nav-launch-status-upcoming   { background: rgba(59,130,246,0.12); color: var(--accent-light); }
+.nav-launch-status-delayed    { background: rgba(251,191,36,0.12); color: #fbbf24; }
+.nav-launch-status-completed  { background: rgba(136,136,136,0.12); color: var(--muted); }
+.nav-launch-status-scrubbed   { background: rgba(224,85,85,0.12);  color: #e05555; }
+.nav-launches-empty { padding: 18px 10px; text-align: center; font-size: 0.82rem; color: var(--muted); }
+.nav-launches-all {
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  gap: 8px;
+  margin-top: 6px;
+  padding: 11px 8px;
+  border-top: 1px solid var(--line);
+  font-size: 0.72rem;
+  font-weight: 700;
+  letter-spacing: 1px;
+  text-transform: uppercase;
+  color: var(--muted);
+  text-decoration: none;
+  transition: color 0.15s;
+}
+.nav-launches-all:hover { color: var(--text); }
+
 .burger {
   display: none;
   flex-direction: column;
@@ -301,6 +423,29 @@ body {
   }
   .nav-links.open { display: flex; }
   .nav-links a { width: 100%; padding: 12px 16px; }
+  .nav-launches { width: 100%; }
+  .nav-launches-toggle { width: 100%; padding: 12px 16px; justify-content: flex-start; }
+  .nav-launches-panel {
+    position: static;
+    width: 100%;
+    max-height: 0;
+    padding: 0 8px;
+    border: none;
+    box-shadow: none;
+    background: transparent;
+    backdrop-filter: none;
+    -webkit-backdrop-filter: none;
+    opacity: 1;
+    transform: none;
+    overflow: hidden;
+    pointer-events: none;
+    transition: max-height 0.22s ease, padding 0.22s ease;
+  }
+  .nav-launches.open .nav-launches-panel {
+    max-height: 480px;
+    padding: 4px 8px 8px;
+    pointer-events: all;
+  }
   .burger {
     display: flex;
     flex-shrink: 0;
diff --git a/shared.js b/shared.js
index bdf5694..ff286de 100644
--- a/shared.js
+++ b/shared.js
@@ -39,6 +39,8 @@ async function injectInclude(placeholderId, url) {
     });
   }
 
+  initNavLaunches();
+
   const currentPath = new URL(window.location.href).pathname.replace(/\/$/, '/index.html');
   document.querySelectorAll('.nav-links a').forEach(a => {
     const href = a.getAttribute('href');
@@ -53,6 +55,94 @@ async function injectInclude(placeholderId, url) {
   injectFooterCredit();
 })();
 
+const LAUNCH_SB_URL = 'https://fqvghuvmgswegirgitom.supabase.co';
+const LAUNCH_SB_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImZxdmdodXZtZ3N3ZWdpcmdpdG9tIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODExMTI4MTAsImV4cCI6MjA5NjY4ODgxMH0.7tKrak3ANnnhp4pISK2ythPdCt557vMACUhpQsqWn0s';
+const NAV_LAUNCHES_LIMIT = 3;
+
+function navEscHtml(str) {
+  return String(str || '').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');
+}
+
+function navLaunchStatusClass(s) {
+  const map = { scheduled:'nav-launch-status-scheduled', upcoming:'nav-launch-status-upcoming',
+                delayed:'nav-launch-status-delayed', completed:'nav-launch-status-completed', scrubbed:'nav-launch-status-scrubbed' };
+  return map[s] || 'nav-launch-status-scheduled';
+}
+
+function renderNavLaunches(listEl, launches) {
+  if (!launches.length) {
+    listEl.innerHTML = '<div class="nav-launches-empty">No upcoming launches.</div>';
+    return;
+  }
+  listEl.innerHTML = launches.map(l => {
+    const date = l.date ? new Date(l.date) : null;
+    const windowEnd = l.window_end ? new Date(l.window_end) : null;
+    const dateStr = date ? date.toLocaleDateString(undefined, { month: 'long', day: 'numeric', year: 'numeric' }) : 'TBD';
+    let timeStr = date ? date.toLocaleTimeString(undefined, { hour: '2-digit', minute: '2-digit' }) : '';
+    if (timeStr && windowEnd && !isNaN(windowEnd.getTime())) {
+      timeStr += ` – ${windowEnd.toLocaleTimeString(undefined, { hour: '2-digit', minute: '2-digit' })}`;
+    }
+    let tzStr = '';
+    if (date) {
+      try {
+        tzStr = Intl.DateTimeFormat(undefined, { timeZoneName: 'long' }).formatToParts(date)
+          .find(p => p.type === 'timeZoneName')?.value || '';
+      } catch { tzStr = ''; }
+    }
+    return `
+      <a class="nav-launch-item" href="/launches.html">
+        <span class="nav-launch-glyph">🚀</span>
+        <span class="nav-launch-info">
+          <span class="nav-launch-name">${navEscHtml(l.name || 'Unnamed Launch')}</span>
+          <span class="nav-launch-date">${dateStr}${timeStr ? ` · ${timeStr}` : ''}</span>
+          ${tzStr ? `<span class="nav-launch-tz">${navEscHtml(tzStr)}</span>` : ''}
+        </span>
+        <span class="nav-launch-status ${navLaunchStatusClass(l.status)}">${navEscHtml(l.status || 'scheduled')}</span>
+      </a>`;
+  }).join('');
+}
+
+async function loadNavLaunches(listEl) {
+  try {
+    const nowIso = new Date().toISOString();
+    const url = `${LAUNCH_SB_URL}/rest/v1/launches?select=name,status,date,window_end&published=eq.true&date=gte.${encodeURIComponent(nowIso)}&order=date.asc&limit=${NAV_LAUNCHES_LIMIT}`;
+    const res = await fetch(url, { headers: { apikey: LAUNCH_SB_KEY, Authorization: `Bearer ${LAUNCH_SB_KEY}` } });
+    if (!res.ok) throw new Error(`API ${res.status}`);
+    const launches = await res.json();
+    renderNavLaunches(listEl, Array.isArray(launches) ? launches : []);
+  } catch (e) {
+    console.warn('nav launches failed to load', e);
+    listEl.innerHTML = '<div class="nav-launches-empty">Unable to load launches.</div>';
+  }
+}
+
+function initNavLaunches() {
+  const widget  = document.getElementById('nav-launches');
+  const toggle  = document.getElementById('nav-launches-toggle');
+  const listEl  = document.getElementById('nav-launches-list');
+  if (!widget || !toggle || !listEl) return;
+
+  function closePanel() {
+    widget.classList.remove('open');
+    toggle.setAttribute('aria-expanded', 'false');
+  }
+
+  toggle.addEventListener('click', (e) => {
+    e.stopPropagation();
+    const isOpen = widget.classList.toggle('open');
+    toggle.setAttribute('aria-expanded', String(isOpen));
+  });
+  document.addEventListener('click', (e) => {
+    if (!widget.contains(e.target)) closePanel();
+  });
+  document.addEventListener('keydown', (e) => {
+    if (e.key === 'Escape') closePanel();
+  });
+
+  loadNavLaunches(listEl);
+  setInterval(() => loadNavLaunches(listEl), 300000);
+}
+
 const DISCORD_STATS_FALLBACK = {
   guild_id: GUILD_ID,
   guild_name: 'ISA- community server',
